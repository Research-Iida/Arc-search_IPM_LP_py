# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version

# celery beat schedule file
celerybeat-schedule

# SageMath parsed files
*.sage.py

# dotenv
.env

# virtualenv
.venv
venv/
ENV/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/

# nohup.out ignore
nohup*.out

# 問題のファイルが大きすぎるので, 圧縮ファイル以外は ignore
data/raw/netlib/*
!data/raw/netlib/.gitkeep
data/processed/*
!data/processed/.gitkeep

# 出力結果は test ディレクトリ以外 commitしなくてよい
# result/test ディレクトリにおいても, 日付ごとに出力が変化するディレクトリは commitしない
result/[1-9]*
result/test/[1-9]*

# profile については実行した環境によって変わるので ignore
profile/*.prof

# 画像は元のファイルがあれば復元可能なので ignore
*.png

# linux 環境と windows 環境で異なるとエラーになるっぽいので ignore
Manifest.toml

config/config_slack.ini
# solver の情報はサーバーと異なるようにしたいので, 別で扱う
config/solver_info.json
